# 化学名称选择功能恢复说明

## 📅 日期
2025-10-14

---

## 🎯 问题描述

### 用户反馈
> "在配方设计里面，从前是可以点击化学名称的时候，双击后有一个小弹窗可以选择我需要的配方，现在怎么没有了"

### 问题分析
步骤二配方设计界面中，双击"化学名称"列时应该弹出化学组分选择对话框，但之前的实现中缺少了这个功能。

---

## ✅ 解决方案

### 1. 添加双击事件处理

**修改位置**: `Gui.py` - `Step2Formula` 类

**原代码**:
```python
# 绑定双击事件进行编辑
self.sheet.bind("<Double-Button-1>", lambda e: self.sheet.open_cell())
```

**新代码**:
```python
# 绑定双击事件 - 化学名称列弹出选择框，其他列正常编辑
self.sheet.bind("<Double-Button-1>", self.on_double_click)
```

---

### 2. 实现双击事件处理函数

```python
def on_double_click(self, event):
    """处理双击事件 - 化学名称列弹出选择框"""
    try:
        # 获取当前选中的单元格
        selected = self.sheet.get_currently_selected()
        if not selected:
            return
        
        row, col = selected[0], selected[1]
        
        # 如果双击的是"化学名称"列（第1列，索引为1）
        if col == 1:
            self.show_chemical_selector(row)
        else:
            # 其他列正常编辑
            self.sheet.open_cell()
    except Exception as e:
        print(f"双击事件错误: {e}")
        self.sheet.open_cell()
```

**功能说明**:
- 检测双击的列
- 如果是"化学名称"列（索引1），弹出选择对话框
- 其他列正常编辑

---

### 3. 实现化学组分选择对话框

```python
def show_chemical_selector(self, row):
    """显示化学名称选择对话框"""
```

**对话框功能**:

#### 🎨 界面设计
- **标题栏**: 蓝色背景，显示"🧪 选择化学组分"
- **搜索框**: 支持按名称或化学式搜索
- **列表框**: 显示所有化学组分（名称 + 化学式）
- **按钮**: 确定、取消

#### 🔍 搜索功能
```python
def update_list(filter_text=""):
    listbox.delete(0, END)
    for item in CHEM_DB:
        name = item["name_cn"]
        formula = item["formula"]
        if filter_text.lower() in name.lower() or filter_text.lower() in formula.lower():
            display_text = f"{name} ({formula})"
            listbox.insert(END, display_text)
```

**特点**:
- 实时搜索过滤
- 支持中文名称和化学式搜索
- 不区分大小写

#### ✅ 选择功能
```python
def on_select(event=None):
    selection = listbox.curselection()
    if not selection:
        return
    
    selected_text = listbox.get(selection[0])
    chem_name = selected_text.split(" (")[0]
    
    chem_info = CHEM_BY_NAME.get(chem_name)
    if chem_info:
        current_data = self.sheet.get_sheet_data()
        if row < len(current_data):
            current_data[row][1] = chem_name  # 化学名称
            current_data[row][2] = chem_info["formula"]  # 化学组成
            current_data[row][3] = chem_info["remark"]  # 备注
            if not current_data[row][4]:
                current_data[row][4] = chem_info.get("default_pct", 0.0)
            
            self.sheet.set_sheet_data(current_data)
    
    dialog.destroy()
```

**自动填充**:
- ✅ 化学名称
- ✅ 化学组成（化学式）
- ✅ 备注（如"氧化剂"、"燃料"等）
- ✅ 默认含量（如果当前为空）

#### 🖱️ 操作方式
1. **双击列表项**: 直接选择并关闭对话框
2. **点击确定按钮**: 选择当前高亮项
3. **点击取消按钮**: 关闭对话框，不做修改

---

## 📊 使用示例

### 操作流程

1. **打开步骤二配方设计**
2. **双击"化学名称"列的任意单元格**
3. **弹出选择对话框**:
   ```
   ┌─────────────────────────────────────┐
   │  🧪 选择化学组分                     │
   ├─────────────────────────────────────┤
   │  🔍 搜索: [____________]            │
   │                                     │
   │  ┌───────────────────────────────┐ │
   │  │ 硝酸铵 (NH4NO3)                │ │
   │  │ 高氯酸铵 (NH4ClO4)             │ │
   │  │ 硝酸羟胺 (NH3OHNO3)            │ │
   │  │ 硝化甘油 (C3H5N3O9)            │ │
   │  │ 黑索金 (C3H6N6O6)              │ │
   │  │ ...                            │ │
   │  └───────────────────────────────┘ │
   │                                     │
   │  [✓ 确定]  [✗ 取消]                │
   └─────────────────────────────────────┘
   ```

4. **搜索化学组分**:
   - 输入"硝酸" → 过滤显示所有包含"硝酸"的组分
   - 输入"NH4" → 过滤显示所有包含"NH4"的组分

5. **选择组分**:
   - 双击"硝酸铵 (NH4NO3)"
   - 或单击选中后点击"确定"

6. **自动填充**:
   - 化学名称: 硝酸铵
   - 化学组成: NH4NO3
   - 备注: 氧化剂
   - 含量: 0.0（可手动修改）

---

## 🎨 对话框设计

### 尺寸和位置
- **大小**: 500x600 像素
- **位置**: 相对于主窗口居中
- **模态**: 阻塞父窗口，必须关闭对话框才能继续

### 配色方案
- **标题栏**: `#0066ff` (蓝色)
- **背景**: `#ffffff` (白色)
- **确定按钮**: `#0066ff` (蓝色)
- **取消按钮**: `#cccccc` (灰色)

### 字体
- **标题**: Microsoft YaHei UI, 16号, 粗体
- **搜索框**: Microsoft YaHei UI, 10号
- **列表**: Microsoft YaHei UI, 10号
- **按钮**: Microsoft YaHei UI, 11号

---

## 🧪 测试清单

### 基本功能
- [ ] 双击"化学名称"列，弹出选择对话框
- [ ] 双击其他列（备注、含量），正常编辑
- [ ] 对话框居中显示
- [ ] 对话框为模态（阻塞父窗口）

### 搜索功能
- [ ] 输入中文名称搜索（如"硝酸"）
- [ ] 输入化学式搜索（如"NH4"）
- [ ] 搜索不区分大小写
- [ ] 搜索实时过滤列表

### 选择功能
- [ ] 双击列表项，自动填充并关闭对话框
- [ ] 单击选中后点击"确定"，自动填充并关闭
- [ ] 点击"取消"，关闭对话框不做修改
- [ ] 按Esc键关闭对话框

### 自动填充
- [ ] 化学名称正确填充
- [ ] 化学组成正确填充
- [ ] 备注正确填充
- [ ] 含量为空时填充默认值
- [ ] 含量不为空时保持原值

### 界面显示
- [ ] 标题栏显示正确
- [ ] 搜索框显示正确
- [ ] 列表框显示所有化学组分
- [ ] 滚动条正常工作
- [ ] 按钮显示正确

---

## 📝 化学组分数据库

### 数据结构
```python
CHEM_DB = [
    {
        "name_cn": "硝酸铵",
        "code": "NH4NO3",
        "formula": "NH4NO3",
        "mw": 80.04,
        "default_pct": 0.0,
        "remark": "氧化剂"
    },
    ...
]
```

### 字段说明
- **name_cn**: 中文名称
- **code**: 机理代码
- **formula**: 化学式
- **mw**: 相对分子质量
- **default_pct**: 默认含量百分比
- **remark**: 备注（氧化剂、燃料、粘合剂等）

### 组分数量
当前数据库包含 **30+** 种常用化学组分

---

## 🎯 优势

### 用户体验提升
1. **快速选择**: 不需要手动输入化学名称
2. **避免错误**: 从列表选择，避免拼写错误
3. **自动填充**: 选择后自动填充相关信息
4. **搜索便捷**: 支持模糊搜索，快速定位

### 数据准确性
1. **标准化**: 所有化学名称和化学式标准化
2. **完整性**: 自动填充化学组成和备注
3. **一致性**: 确保数据来源统一

---

## 🔧 代码修改总结

### 修改的文件
- `Gui.py`

### 修改的类
- `Step2Formula`

### 新增方法
1. **`on_double_click(event)`** - 双击事件处理
2. **`show_chemical_selector(row)`** - 显示选择对话框

### 修改的方法
- 双击事件绑定（第891-893行）

---

## ✅ 完成状态

- ✅ 双击事件处理函数
- ✅ 化学组分选择对话框
- ✅ 搜索功能
- ✅ 自动填充功能
- ✅ 界面美化
- ⏳ 等待用户测试

---

**下一步**: 请测试步骤二配方设计中的化学名称选择功能！

**测试方法**: 
1. 启动程序
2. 进入步骤二
3. 双击"化学名称"列的任意单元格
4. 查看是否弹出选择对话框
5. 测试搜索和选择功能

---

**燃烧速度仿真软件 v2.0**  
**© 2025 All Rights Reserved**

